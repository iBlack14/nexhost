import SectionPage from '@/components/layout/SectionPage'

export default function SslPage() {
  return (
    <SectionPage
      title="SSL"
      description="Gestiona certificados SSL y verifica su vigencia por dominio."
    />
  )
}
